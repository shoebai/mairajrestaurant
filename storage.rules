rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Anyone can view uploaded photos (needed for the public website).
    // Only a signed-in admin can upload or delete.
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
